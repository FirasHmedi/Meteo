import { StyleSheet } from "react-native";

export const boxStyles = StyleSheet.create({
	container: {
		padding: 10,
		borderColor: 'black',
		borderWidth: 1,
		height: 200,
		margin: 10,
		justifyContent: 'center',
		alignItems: 'center',
	},
	text: {
		fontSize: 18,
	},
});
